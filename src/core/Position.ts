import GameObject from "./GameObject";

export default class Position {
    x: number = 0;
    y: number = 0;
    // width: number;
    // height: number;
    // rotate: number;
    // scale: number;
}